# v0.7.6 Final Audit Response

This record maps the independent post-release audit to the focused v0.7.7
maintenance release. v0.7.6 remains live while v0.7.7 is built, signed, tested,
and exercised independently. No calibration-sensitive threshold, weight, or
predictive claim changed.

## High Findings

| Finding | v0.7.7 disposition | Evidence |
| --- | --- | --- |
| SQLite read-only wording omitted WAL/SHM sidecars | Fixed: logical read-only wording plus open, journal, possible-sidecar, and observed-sidecar fields | Windows closed-WAL directory comparison and main-file hash regression |
| Rule classification reparsed blocks with double-quote regexes | Fixed: named Starlark arguments, top-level pattern positions, nested alternatives, decision semantics, execution flags in any later position, missing operands, and raw Windows paths | Required PowerShell, Cmd, shell, Python, Node, option displacement, missing/fixed-file, raw-path, quote, reorder, nesting, escape, and comment fixtures |
| Stable-correlation approval retries were discarded | Fixed: exact records, exact same-time cross-schema mirrors, and later same-ID requests are separate | Stable-ID retry plus cross-schema mirror regression |
| Rule-miss advice did not require resolved allowed reviews | Fixed: at least two equivalent requests must each have an explicit `ALLOW`, terminal resolution, and supported prefix; prior decisions are never inherited | 307-allow positive fixture plus inherited-decision, prefix-unavailable, denied, and unresolved negative fixtures |

## Medium Findings

| Finding | v0.7.7 disposition | Evidence |
| --- | --- | --- |
| Successful plans reported `capacity_reserved=false` | Fixed | Governor pending-plan capacity assertions |
| Skill wording conflated a persistence write error with a runaway | Fixed: diagnoses remain separate | Skill and release source checks |
| Session enumeration could eagerly materialize a large directory | Fixed: streaming enumeration, three-second target, 20,000-entry hard cap | Inspector source and output assertions |
| Large-rollout head timestamp did not drive task age | Fixed with explicit filesystem fallback count | Sparse large-file head regression |
| Unknown outcomes reduced allow percentage | Fixed: unknown counter; denominator is known allow plus deny | Mixed known/unknown decision regression |
| Resolution state never reported complete outcomes | Fixed: complete, partial, and no-outcome states | Fully resolved and partial fixtures |
| Exiting processes could abort sampling | Fixed: per-process property isolation and partial-sample confidence | Parser/source guard plus repeated live inspections |
| V1 `fork_context=false` was incomplete | Fixed: false is none; missing or malformed is unknown | V1 two-record regression |
| Manifest publisher conflicted with policy identity | Fixed: author and developer are Dravara, LLC; FaxanFM remains the repository account | Manifest and release validation |

## Documented Limits

- Filesystem-helper detection remains limited to known timestamped failure
  markers and reports that scope.
- Marginal interval totals remain selected-window observations, not billing.
- Governor fingerprints remain a Git-visible projection, not proof of zero
  filesystem effects.
- Governor state remains local, unauthenticated, and untrusted coordination
  metadata.
- Release reproducibility proves same-environment Windows byte identity; it
  does not claim equivalence across arbitrary ZIP implementations.
- The immutable v0.7.6 tag and assets are not rewritten. The authoritative ZIP
  SHA-256 is recorded as an erratum in the publication and operations records.

## Publication Record

Chronos v0.7.6 is published in the OpenAI Plugins Directory by Dravara, LLC:
https://chatgpt.com/plugins/plugins_6a79c882cf488191b8f62ee20e0e2571

The retained evidence does not establish exact submission, approval,
publication, or selected-region values. Those fields are recorded as unknown,
not guessed. Directory publication is distribution approval, not OpenAI
endorsement.

## Validation Boundary

The immutable v0.7.6 tag contains 36 deterministic Governor validations.
v0.7.7/current main contains 42 Governor validations plus the expanded
Inspector and release suites. v0.7.7 is not validated until another installation
machine successfully exercises the packaged version.
